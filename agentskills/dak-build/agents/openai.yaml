interface:
  display_name: "DAK Build"
  short_description: "Build Delphi and TMS WEB Core projects via DelphiAIKit"
  default_prompt: "Build this Delphi or TMS WEB Core project from Windows or WSL using DelphiAIKit.exe build as the canonical interface, use the target platform explicitly, and report actionable diagnostics."
